<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
      <h3 class="test-3xl mb-3">Create User</h3>
      <form method="post" action="/create">
        <input
          class="px-3 py-2 rounded-md bg-transparent border-2 border-2 border-zinc-800 outline-non"
          type="text"
          placeholder="username"
          name="username"
        />
        <input
          class="px-3 py-2 rounded-md bg-transparent border-2 border-2 border-zinc-800 outline-non"
          type="email"
          placeholder="email"
          name="email"
        />
        <input
          class="px-3 py-2 rounded-md bg-transparent border-2 border-2 border-zinc-800 outline-non"
          type="password"
          placeholder="password"
          name="password"
        />
        <input
          class="px-3 py-2 rounded-md bg-transparent border-2 border-2 border-zinc-800 outline-non"
          type="text"
          placeholder="age"
          name="age"
        />
        <input
          class="px-5 py-2 rounded-md bg-blue-500"
          type="submit"
          value="create User"
        />
      </form>
    </div>
  </body>
</html>
